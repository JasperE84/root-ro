diff a/reboot-to-readonly-mode.sh b/reboot-to-readonly-mode.sh	(rejected hunks)
@@ -1,3 +1,7 @@
 #/bin/sh
 rm /disable-root-ro
+systemctl stop systemd-random-seed.service
+if [ -f /var/lib/systemd/random-seed ]; then
+  rm -f /var/lib/systemd/random-seed
+fi
 reboot
